{
  "name": "casual-golfers-tour",
  "private": true,
  "version": "1.0.0",
  "description": "Casual Golfers Tour - a fun, colorful golf tour tracker for a group of 12 friends",
  "scripts": {
    "dev": "concurrently -n SERVER,CLIENT -c green,magenta \"npm run dev --prefix server\" \"npm run dev --prefix client\"",
    "install:all": "npm install --prefix server && npm install --prefix client",
    "build": "npm run build --prefix client",
    "seed": "npm run seed --prefix server"
  },
  "devDependencies": {
    "concurrently": "^9.1.2"
  }
}
